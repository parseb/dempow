<%= form_for @post, :url => url_for(:controller => 'posts', :action => 'create') do |f| %>
							<%= f.label :title %>
							<%= f.text_field :title %><br />
							<%= f.label :body %>
							<%= f.text_area :body %>
							<br /> 
							<%= f.submit "Post" %> 
							<% end %>